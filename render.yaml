services:
  - type: web
    name: cafe-system
    env: node
    buildCommand: npm install
    startCommand: cd backend && node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
